#!/bin/bash    
# this file contains all commands to compile and install the daemon

./autogen.sh
./configure --enable-tests=no --without-gui --with-incompatible-bdb CFLAGS="-march=native"
make
make install
